os: linux
sudo: required
language:
  - minimal
services:
  - docker

jobs:
  include:
    - name: "all projects"
      script:
        - cmake -H. -B_build
        - cmake --build _build

notifications:
  email: false

branches:
  except:
    - master
